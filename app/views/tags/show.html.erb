<h1>Articles tagged with tag name: <%= @tag.name %> </h1>

<ul>
  <% @tag.articles.each do |article| %>
    <li><%= link_to article.title, article_path(article) %></li>
  <% end %>
</ul>

<br/><h3><%= link_to "View All Tags", tags_path(tag) %></h3>